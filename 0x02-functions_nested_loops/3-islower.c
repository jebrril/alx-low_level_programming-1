#include <stdio.h>
/**
* _islower -  checks for lowercase character.
*
* Return : Always 0 (success)
*/
void print_alphabet_x10(void){
if (c >= 'a' && c <= 'z') {
return (1);
} else {
return (0);
}
}
