#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 * Description: checks on the last digit of the assigned var
 * Return: Always 0 (success)
 */
int main()
{
int i;
for (i = 97; i <= 122; i++)
{
	putchar(i);
}
putchar('\n');
return 0;
}
